= form_for(@boat) do |f|
  - if @boat.errors.any?
    #error_explanation
      %h2
        = pluralize(@boat.errors.count, "error")
        prohibited this boat from being saved:
      %ul
        - @boat.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :boat_image
    %br/
    = f.text_field :remote_boat_image_url
  .field
    = f.label :description
    %br/
    = f.text_field :description
  .actions
    = f.submit "Change boat"
